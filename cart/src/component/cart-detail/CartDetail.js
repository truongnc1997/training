import React, { Component } from "react";
import { CartContext } from "../../context/Cart";

class CartDetail extends Component {
  render() {
    return (
      <div>
        {/* <CartContext.Consumer>
          {({ cartItem }) => <div>({cartItem.id})</div>}
        </CartContext.Consumer> */}
        
      </div>
    );
  }
}

export default CartDetail;
